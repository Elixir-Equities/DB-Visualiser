# Backend URL — server-side only, never sent to the browser.
# Used by the Vite dev proxy and the nginx container at runtime.
# Do NOT prefix with VITE_ — that would expose it in the JS bundle.
BACKEND_URL=http://localhost:8000
